# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

participantCount: 2
tlsSecret: testing-cluster
fullDomain: testing-cluster.us-east.containers.appdomain.cloud
image:
  repository: ibmappmodernization/web-terminal
  tag: 0.1.1
  pullPolicy: Always
